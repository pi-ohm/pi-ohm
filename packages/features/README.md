# @pi-ohm/features

Focused feature package for Pi Ohm.

Current scope:

- handoff
- subagents
- session/thread search
- handoff visualizer
- painter/imagegen

This package wires commands/status/widgets and consumes typed config from `@pi-ohm/config`.
