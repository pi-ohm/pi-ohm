export const HandoffFeature = {
  slug: "handoff",
  path: "packages/features/src/features/handoff",
  summary: "Implements focused handoff flows (manual + agent-invoked) for splitting long threads into goal-oriented sessions.",
} as const;
