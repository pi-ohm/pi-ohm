export const SubagentsFeature = {
  slug: "subagents",
  path: "packages/features/src/subagents",
  summary: "Adds subagent delegation adapters (interactive-shell / plugin) for parallel or isolated task execution.",
} as const;
