# @pi-ohm/core TODO

- Keep package minimal and tree-shakeable.
- Expand only with primitives that are domain-agnostic and expected to be reused by 2+ packages.
- Maintain typed `better-result` error model.
