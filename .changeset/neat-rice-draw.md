---
"@pi-phm/subagents": patch
"pi-ohm": patch
---

Scaffolded dedicated subagent profiles in `@pi-phm/subagents` for:

- Librarian
- Oracle
- Finder
- Task
- Painter

Added `/ohm-subagent <id>` for inspecting a specific subagent profile and expanded `/ohm-subagents` to show the full catalog.
